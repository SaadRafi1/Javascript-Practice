let num1 = parseInt(prompt("Enter the first integer:"));
let num2 = parseInt(prompt("Enter the second integer:"));

sum = num1 + num2;

if (sum >= 50 && sum <= 80) {
  console.log(
    "The sum is either equal to 50 or 80 or  between 50 and 60:" + 65
  );
} else {
  console.log("The sum is not between 50 and 60:" + 80);
}
